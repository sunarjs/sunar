/**
 * Limit of embeds that can be included in a single message.
 * @readonly
 */
export const MessageEmbedsLimit = 10;

/**
 * Limit of reactions that can be added to a single message.
 * @readonly
 */
export const MessageReactionsLimit = 20;

/**
 * Limit of action rows that can be included in a single message.
 * @readonly
 */
export const MessageActionRowsLimit = 5;

/**
 * Total character limit allowed in a single message.
 * @readonly
 */
export const MessageCharactersLimit = 2000;

/**
 * Maximum size limit (in bytes) for a message request.
 * @readonly
 */
export const MessageRequestSizeLimit = 25000000;

/**
 * Total character limit allowed in a single message for users with Discord Nitro.
 * @readonly
 */
export const MessageCharactersNitroLimit = 4000;

/**
 * Limit of roles mentioned in a single message's allowed mentions.
 * @readonly
 */
export const MessageAllowedMentionsRolesLimit = 100;

/**
 * Limit of users mentioned in a single message's allowed mentions.
 * @readonly
 */
export const MessageAllowedMentionsUsersLimit = 100;

/**
 * Limit of reactions that can be fetched for a single message.
 * @readonly
 */
export const MessageReactionsFetchLimit = 100;
